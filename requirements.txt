pyyaml>=6.0
openai>=1.30.0
rich>=13.0.0
click>=8.1.0

# Full featured web dashboard + extras (what you need for `logsentinel run --web`)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
jinja2>=3.1.0
itsdangerous>=2.0
ldap3>=2.9
python-multipart>=0.0.6
geoip2>=4.8
pyotp>=2.9
qrcode>=7.4
bcrypt>=4.0.0
requests>=2.31.0          # required for online geo IP fallback (used on Maps page for missing public IPs)

# Note: the package itself is best installed with `pip install -e ".[web]"` (or -e ".")
# This file is a convenience for people who can't/won't use the editable install.
